---
- name: "Read data files"
  hosts: all
  vars_files:
    - "config.json"

  tasks:
    - name: save the Json data to a Variable as a Fact
      set_fact:
        system: "{{ system }}"
        cacheable: yes
